data/*
!data/dev_meta/
!data/avail/
!data/enrich/
!data/pro/
data/pro/*
!data/pro/symphony_los.json
data/dev_meta/*
!data/dev_meta/curated/
# 15 Sep 2026: the sales-label -> DLD area alias table is hand- and register-checked mapping that the yields depend on, not a
# rebuildable output - tracked (39 register-backed corrections applied that day)
!data/dld/
data/dld/*
!data/dld/area_alias.json
# 15 Sep 2026 (digital thread P1.2): hand decisions on board developer -> DLD developer entity links win over the rules - tracked
!data/identity/
data/identity/*
!data/identity/developer_group_decisions.json
.env
__pycache__/
public/heatmap_story.png
public/heatmap_square.png
naj.duckdb
*.log

# 16 Sep 2026: client sheets. The _facts files are hand-curated from each developer's own brochure -
# a decision about what a client sheet may claim, not a rebuildable output - so they are tracked.
# The rendered sheets, the median cache and the picture assets all rebuild from source and are not.
!data/sheets/
data/sheets/*
!data/sheets/_facts/

# The per-project manifests naming which developer URLs a building's pictures came from are the
# audit trail behind every image on a client sheet - tracked. The images themselves re-fetch.
!data/media/
data/media/*
!data/media/_sources/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

# 17 Sep 2026: Azimuth EV charging map. The scripts, the page source and the credentials request are
# tracked; everything they emit rebuilds from them and is not. Rebuild with:
#   python scripts/ocm_pull.py pull && python scripts/ocm_pull.py load
#   python scripts/osm_pull.py pull && python scripts/osm_pull.py load
#   python scripts/supercharge_pull.py pull && python scripts/supercharge_pull.py load
#   python scripts/build_ev_union.py --export && python scripts/build_ev_app.py
#   python scripts/render_ev_map.py
public/ev_app_data.json
public/ev_app_standalone.html
public/ev_map_square.png
public/ev_map_story.png
dist_ev/
# The CGA rule is hand-written source for CityEngine, not a rebuildable output - tracked.
!data/ev/
data/ev/*
!data/ev/cityengine/
data/ev/cityengine/*
!data/ev/cityengine/EVCharger.cga
# The rule package is a build output of build_ev_rpk.py, not source.
data/ev/cityengine/EVCharger.rpk
public/ev_archetypes/
